public boolean sleepIn(boolean weekday, boolean vacation) {
  if (weekday == true && vacation == true || weekday == false && vacation == true)
    return true;
  else if (weekday == false && vacation == false)
    return true;
  else
    return false;
}
